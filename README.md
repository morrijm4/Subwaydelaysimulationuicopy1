
  # Subway Delay Simulation UI (Copy)

  This is a code bundle for Subway Delay Simulation UI (Copy). The original project is available at https://www.figma.com/design/HBN0YbZ3nQItt2PBxD7bQq/Subway-Delay-Simulation-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  